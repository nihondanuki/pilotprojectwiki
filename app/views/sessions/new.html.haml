.container
  %h1 ログイン

  - flash.each do |key,value|
    = content_tag(:div, value, class: "#{key}")
  = form_for :session, url: login_path do |f|
    .form.form-group
      = f.label :email
      = f.email_field :email, class: "form-control"
    .form.form-group
      = f.label :password
      = f.password_field :password, class: "form-control"
    .actions
      = f.submit 'Login'

  %p ユーザー登録
  = link_to "ログイン", "users/new"
